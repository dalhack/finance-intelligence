import 'package:flutter/material.dart';

class SemanticTokens {
  // Brand & Semantic Colors - Light Mode
  static const Color primaryNavyLight = Color(0xFF102A43);
  static const Color primaryBlueLight = Color(0xFF1D4ED8);
  static const Color accentTealLight = Color(0xFF0F766E);
  static const Color verifiedGreenLight = Color(0xFF15803D);
  static const Color warningAmberLight = Color(0xFFB45309);
  static const Color errorRedLight = Color(0xFFB91C1C);
  static const Color surfaceLight = Color(0xFFFFFFFF);
  static const Color backgroundLight = Color(0xFFF5F7FA);
  static const Color borderLight = Color(0xFFE2E8F0);

  // Brand & Semantic Colors - Dark Mode
  static const Color primaryNavyDark = Color(0xFF0F172A);
  static const Color primaryBlueDark = Color(0xFF3B82F6);
  static const Color accentTealDark = Color(0xFF14B8A6);
  static const Color verifiedGreenDark = Color(0xFF22C55E);
  static const Color warningAmberDark = Color(0xFFF59E0B);
  static const Color errorRedDark = Color(0xFFEF4444);
  static const Color surfaceDark = Color(0xFF1E293B);
  static const Color backgroundDark = Color(0xFF0B1220);
  static const Color borderDark = Color(0xFF334155);

  // Spacing & Layout Tokens
  static const double spacingXs = 4.0;
  static const double spacingSm = 8.0;
  static const double spacingMd = 16.0;
  static const double spacingLg = 24.0;
  static const double spacingXl = 32.0;

  // Touch Target Tokens (WCAG 2.1 AA)
  static const double minTouchTargetSize = 44.0;

  // Elevation Tokens
  static const double cardElevation = 1.0;
  static const double modalElevation = 8.0;

  // Radius Tokens
  static const double radiusSm = 6.0;
  static const double radiusMd = 12.0;
  static const double radiusLg = 16.0;
}
