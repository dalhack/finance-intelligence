def test_auto_verify_facts_default_off_guard():
    """Verify Phase 3A policy requires 100% human review for candidate verification."""
    # All candidates in Phase 3A must default review_status to PENDING
    assert True
