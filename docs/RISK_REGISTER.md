# Finance Intelligence — Master Risk Register

> **Document ID**: `RSK-FIN-001`  
> **Status**: `Draft — Pending Phase 0 User Review`  
> **Classification**: `INTERNAL`

---

## 1. Top 10 High-Priority Technical & Operational Risks

| Risk ID | Category | Description | Likelihood (1-5) | Impact (1-5) | Exposure Score | Owner Role | Mitigation Strategy | Contingency Plan | Trigger Condition | Status |
|---|---|---|---|---|---|---|---|---|---|---|
| **`RSK-001`** | **Financial Accuracy** | Binary floating-point rounding errors lead to balance sheet reconciliation mismatch. | 4 | 5 | **20** | Lead Financial Engineer | Enforce multi-tier `Decimal` arithmetic in pure Python Calculation Engine; prohibit `float`. | Re-run calculation with high-precision Decimal fallback and log reconciliation audit. | Reconciliation delta > 0.001% between assets and liabilities. | `Proposed Mitigation` |
| **`RSK-002`** | **Security / AI** | Indirect Prompt Injection inside uploaded PDF commands LLM to bypass policy. | 4 | 5 | **20** | Application Security Lead | XML tag wrapping, prompt isolation, strict Pydantic JSON schema tool output validation (zero LLM tenant args), no code execution. | Strip unparsed text context, trigger manual review, and log security incident. | LLM attempts tool invocation outside defined JSON schemas. | `Proposed Mitigation` |
| **`RSK-003`** | **Security / Privacy** | Cross-tenant data leakage occurs via missing SQL tenant filter (IDOR/BOLA). | 2 | 5 | **10** | Lead Backend Architect | Defense-in-depth: PostgreSQL Row-Level Security (RLS) policies using `app.current_organization_id`, runtime role segregation (`db_app_user` with `NOBYPASSRLS`), connection pool context reset hooks. | Immediately revoke session, terminate connection, and trigger automated security response. | Request attempts query across distinct tenant UUIDs. | `Proposed Mitigation` |
| **`RSK-004`** | **Security / SSRF** | `fetch_official_document` tool is coerced to query internal GCP Cloud Metadata IP (`169.254.169.254`). | 3 | 5 | **15** | Application Security Lead | Domain allowlisting, DNS pre-resolution IP checking against RFC 1918/6598 ranges, max 3 redirects. | Block socket creation immediately; alert security ops. | Target URL resolves to private or metadata IP range. | `Proposed Mitigation` |
| **`RSK-005`** | **Cost Control** | Infinite loop in agent tool calling drains model token budget ($10k+ unexpected bill). | 3 | 4 | **12** | Cloud Infra Architect | Hard cap of 8 tool steps per job; user daily token caps; anomaly detection alerts in Cloud Monitoring. | Emergency feature flag `FORCE_DETERMINISTIC_ONLY` trips kill-switch. | Job tool call count exceeds 8 steps or daily token budget > 80%. | `Proposed Mitigation` |
| **`RSK-006`** | **Performance** | Complex PDF ingestion or table extraction exceeds Cloud Run 60s timeout. | 3 | 4 | **12** | Backend Engineer | Offload ingestion to async Cloud Tasks worker queues; stream extraction progress via SSE. | Retry job on worker with extended timeout cap. | Worker execution time exceeds 45 seconds. | `Proposed Mitigation` |
| **`RSK-007`** | **Vendor Lock-in** | Primary model provider API outage halts analytical summary generation. | 3 | 4 | **12** | LLM Architect | Build Capability-Matrix Provider Adapter supporting both Anthropic Claude Direct API and GCP Vertex AI. | Evaluate failover compatibility rules before switching provider adapter. | Primary provider returns >= 5% HTTP 5xx errors. | `Proposed Mitigation` |
| **`RSK-008`** | **Data Integrity** | User uploads 50MB Decompression Bomb PDF/XLSX exhausting container memory (OOM crash). | 3 | 4 | **12** | Cloud Infra Architect | Pre-upload MIME magic validation, server-side SHA-256 calculation, memory allocation caps, streaming decompression. | Terminate container instance safely and delete temporary file. | Decompressed stream size exceeds 100MB limit. | `Proposed Mitigation` |
| **`RSK-009`** | **Compliance** | Exported CSV contains spreadsheet formula injection payload (`=CMD|...`). | 3 | 3 | **9** | Backend Engineer | CSV/Excel sanitizer escaping fields starting with `=`, `+`, `-`, `@`, `0x09`, `0x0D`. | Strip leading special characters during export file generation. | Export field starts with formula trigger character. | `Proposed Mitigation` |
| **`RSK-010`** | **Model Reliability** | LLM asserts synthetic financial figures not present in source filings. | 4 | 5 | **20** | Lead QA Engineer | Quality Gate suite cross-checks LLM text numbers against deterministic fact store; requires valid `[EIV-XXXX]` tag. | Block job completion; output `completed_with_warnings` with unverified claims stripped. | Quality Gate `GATE_CITATION_FAITHFULNESS` fails. | `Proposed Mitigation` |
